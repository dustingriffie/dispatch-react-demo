const adminClaims = {
    admin: true
  };
  
  const technicianClaims = {
    technician: true
  };
  
  const customerClaims = {
    customer: true
  };

  module.exports = {adminClaims, technicianClaims, customerClaims};